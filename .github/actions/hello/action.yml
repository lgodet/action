name: "Custom action"
  
description: "Greet someone"   
inputs: 
  name_input:
    description: "the name"
    required: true
runs:
  using: "composite"
  steps: 
    - name: "Great the user"
      run: echo "Hello ${{ inputs.name_input }}."
      shell: bash

    - name: Affichage du path dans checkout action
      run: pwd
      shell: bash
      
